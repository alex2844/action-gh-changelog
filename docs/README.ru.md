# 📜 Генератор Changelog

Легкий и мощный CLI инструмент для автоматической генерации красивых списков
изменений (changelog) на основе истории Git и соглашения
[Conventional Commits](https://www.conventionalcommits.org/).

Проект создан быть портативным, быстрым и удобным для использования
как локально, так и в CI/CD (GitHub Actions).

[🇺🇸 Read in English](../README.md)

## ✨ Возможности

- **Без зависимостей**: Написан на чистом Bash. Требует только `git`
    (и `curl`/`jq` для работы с API).
- **Автоматический диапазон**: Сам находит последний тег и генерирует
    список для новых изменений.
- **Умная группировка**: Группирует коммиты по типам
    (`feat`, `fix`, `perf` и т.д.).
- **Поддержка Squash**: Корректно "распаковывает" Squashed-коммиты,
    объединяя их под одним хешем для чистоты истории.
- **Интеграция ссылок**: Умеет генерировать ссылки на коммиты и авторов
    (используя GitHub API).
- **Портативность**: Легко устанавливается одной командой.

## 🚀 Установка

### CLI (Linux/macOS/WSL)

Установка последней версии в `~/.local/bin` одной командой:

<!-- markdownlint-capture -->
<!-- markdownlint-disable MD013 -->
```bash
curl -fsSL https://raw.githubusercontent.com/alex2844/action-gh-changelog/main/install.sh | bash
```
<!-- markdownlint-restore -->

Или скачайте готовый файл со страницы [Releases][releases].

### GitHub Actions

Вы можете использовать этот инструмент в своих workflow:

```yaml
- name: Generate Changelog
  uses: alex2844/action-gh-changelog@v1
  with:
    output: 'RELEASE_NOTES.md'
    # lang: 'ru' # Опционально: язык заголовков (en/ru)
```

#### Параметры Action

| Параметр | Описание | По умолч. |
| :--- | :--- | :--- |
| `output` | Путь к файлу вывода. Если не задан, выводит в stdout. | - |
| `tag` | Тег для генерации. | `latest` |
| `since` | Начальная дата/ссылка. | - |
| `until` | Конечная дата/ссылка. | - |
| `links` | Добавлять ссылки на хеши и авторов. | `true` |
| `raw` | Простой список без группировки. | `false` |
| `lang` | Язык заголовков (`en`, `ru`). | `en` |

## 📋 Использование CLI

```bash
changelog [ОПЦИИ]
```

### Флаги

| Флаг | Полный флаг | Описание |
| :--- | :--- | :--- |
| `-t` | `--tag` | Тег для генерации (по умолч.: последний). |
| `-o` | `--output` | Файл для сохранения (по умолч.: экран). |
| `-s` | `--since` | Начальная дата (`'2025-01-01'`). |
| `-u` | `--until` | Конечная дата. |
| `-l` | `--links` | Добавлять ссылки к хешам и авторам. |
| `-r` | `--raw` | Простой список без группировки. |
| `-q` | `--quiet` | Тихий режим (только ошибки). |
| `-v` | `--version` | Показать версию. |
| `-h` | `--help` | Показать справку. |

### Примеры

**1. Сгенерировать changelog для невыпущенных изменений:**

```bash
changelog
```

**2. Сгенерировать описание для тега с ссылками и сохранить в файл:**

```bash
changelog -t v1.0.0 -l -o notes.md
```

**3. Получить простой список коммитов за последний месяц:**

```bash
changelog --since "1 month ago" --raw
```

## 📝 Соглашение о коммитах

Для корректной группировки скрипт ожидает, что ваши коммиты следуют
соглашению [Conventional Commits](https://www.conventionalcommits.org/).
Вот основные типы, которые распознает скрипт:

- **`feat`** (🚀 Новые возможности): Новая функциональность.
- **`fix`** (🐛 Исправления): Исправление ошибки.
- **`refactor`** (✨ Улучшения и оптимизация): Изменение кода, которое
    не исправляет баг и не добавляет фичу.
- **`perf`** (✨ Улучшения и оптимизация): Изменение кода, улучшающее
    производительность.
- **`revert`** (⏪ Отмененные изменения): Откат предыдущего коммита.
- **`docs`** (📖 Документация): Изменения, касающиеся только документации.
- **`ci`** (⚙️ CI/CD): Изменения в файлах конфигурации и скриптах CI/CD.
- **`chore`** (🔧 Прочее): Другие изменения, не затрагивающие исходный
    код или тесты.

## 🤝 Участие в проекте

Мы приветствуем любой вклад! Если у вас есть идеи, предложения или вы
нашли ошибку, пожалуйста, создайте [Issue][issues].\
Если вы хотите помочь с кодом, мы будем рады вашим [Pull-запросам][pulls].

[issues]: https://github.com/alex2844/action-gh-changelog/issues
[pulls]: https://github.com/alex2844/action-gh-changelog/pulls
[releases]: https://github.com/alex2844/action-gh-changelog/releases
